const defaultState = {
  article: {}
}

const articleReducer = (state = defaultState, action) => {
  // if (action.type === "ARTICLES") {
  //   return {...state, news: }
  // }

  return state
}

export default articleReducer
